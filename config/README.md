This folder contains placeholders for future service configuration.

- firebase.ts: Export placeholders for Firebase project IDs, API keys, etc.
- payments.ts: Stripe/PayPal sandbox keys and settings.
- maps.ts: Google Maps / Mapbox tokens.

Do not commit real secrets. Use environment variables or secure storage when integrating.

